version: "3"

services:
  postgres:
    image: postgres:10.0-alpine
    container_name: mybooks-postgres
    volumes:
      - /home/rpg/docker/mybooks-postgres:/var/lib/postgresql/data
    restart: always
    networks:
      - books

  app:
    build: .
    container_name: mybooks-server
    ports:
      - "3333:3333"
    volumes:
      - .:/usr/app
      - /home/rpg/docker/nginx/mybooks:/usr/app/public/images
    command: yarn prod
    restart: always
    networks:
      - books

networks:
  books:
    driver: bridge
